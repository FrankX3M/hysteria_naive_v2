---
name: 🐛 Bug report
about: Сообщить об ошибке в установке или работе проекта
title: "[Bug] "
labels: bug
assignees: ''
---

## Описание проблемы

Кратко: что пошло не так.

## Как воспроизвести

1. Команда/шаг 1 (например: `sudo ./install.sh --config install.env`)
2. Команда/шаг 2
3. ...

## Ожидаемое поведение

Что должно было произойти.

## Фактическое поведение

Что произошло на самом деле. Приложите вывод ошибки, если есть.

## Окружение

- ОС и версия: (например, Debian 12 / Ubuntu 24.04)
- Архитектура: amd64 / arm64
- Версия проекта / коммит: (`git rev-parse HEAD` или тег релиза)
- Вывод `proxy-admin status`:

```
вставьте сюда вывод
```

## Логи

```
journalctl -u hysteria-server -n 50 --no-pager
journalctl -u proxy-authd -n 50 --no-pager
```

Вставьте релевантные строки (без секретов — токены, пароли и IP клиентов лучше замаскировать).

## Дополнительный контекст

Что-то ещё, что может быть важно (кастомные патчи, `chain-route-setup.sh` применён или нет, и т.п.).
